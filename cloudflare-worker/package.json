{
  "name": "halalelectronics-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Unified Cloudflare Worker for Halal Electronics (SMS + Telegram + admin auth)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
